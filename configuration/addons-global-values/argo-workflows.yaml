# Argo Workflows Global Values
server:
  authModes:
    - server
    - client
controller:
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      memory: 512Mi
  parallelism: 50
