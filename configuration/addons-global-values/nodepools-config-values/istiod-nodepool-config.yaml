# ---- Istiod EKS Auto Mode infra-karpenter Overrides ---- #
pilot:
  nodeSelector:
    node-type: infrastructure
  tolerations:
    - key: infrastructure
      operator: Equal
      value: "true"
      effect: NoSchedule
