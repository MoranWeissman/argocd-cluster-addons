# Argo Events Global Values
controller:
  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      memory: 512Mi
configs:
  jetstream:
    streamConfig:
      maxBytes: 2GB
      replicas: 3
