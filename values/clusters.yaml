clusters:

# | -- Environment: Dev -- | #

  # Other clusters
  - name: cluster-1
    labels:
      env: dev
      datadog: enabled
      datadog-version: "3.70.7"
      keda: enabled
      external-secrets: enabled
